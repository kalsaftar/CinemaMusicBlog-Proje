<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CinemaMusicBlog</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        /* 1. RENK PALETİ (KEMİK/KREM & NEON KIRMIZI) */
        :root {
            --bg-dark: #0a0a0a;
            --card-dark: #16181d;
            --neon-red: #ff0055;
            --neon-blue: #00d2ff;
            --text-cream: #d4d4d4; /* O istediğin şık krem rengi */
        }

        /* 2. ANA YERLEŞİM */
        html, body {
            background-color: var(--bg-dark) !important;
            color: var(--text-cream) !important;
            font-family: 'Inter', sans-serif;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }

        body {
            padding-top: 75px; /* Navbar'ın altında kalan içerik için */
        }

        a, a:hover {
            text-decoration: none !important;
        }

        /* 3. NAVBAR (KOMPAKT VE ŞIK) */
        .navbar-custom {
            background: rgba(10, 10, 10, 0.98) !important;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
            padding: 0.5rem 0 !important;
            height: 65px;
        }

        .navbar-brand {
            font-weight: 800;
            font-size: 1.3rem !important;
            background: linear-gradient(90deg, var(--neon-red), var(--neon-blue));
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-right: 1.5rem !important;
        }

        .nav-link {
            color: var(--text-cream) !important;
            font-size: 13px;
            letter-spacing: 1px;
            font-weight: 700;
            text-transform: uppercase;
            display: flex;
            align-items: center;
            gap: 6px;
            transition: 0.3s ease;
        }

            .nav-link:hover {
                color: var(--neon-red) !important;
                text-shadow: 0 0 10px var(--neon-red);
            }

        /* 4. İÇERİK ÖLÇEĞİ */
        main h1, main h2 {
            color: var(--text-cream) !important;
            font-weight: 800;
            letter-spacing: -0.5px;
        }

        main p {
            color: var(--text-cream) !important;
            font-size: 0.95rem;
            opacity: 0.9;
        }

        main .card {
            background-color: var(--card-dark) !important;
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 4px; /* Keskin ve modern köşeler */
        }

        /* 5. ŞEFFAF ÇERÇEVELİ BUTONLAR (İSTEDİĞİN AYNI TASARIM) */
        main .btn, main .btn-primary, main .btn-outline-primary, main a.btn {
            background-color: transparent !important; /* İÇİ ŞEFFAF */
            color: var(--neon-red) !important; /* YAZI KIRMIZI */
            border: 1px solid var(--neon-red) !important; /* ÇERÇEVE KIRMIZI */
            border-radius: 50px !important;
            font-size: 11px !important;
            font-weight: 800 !important;
            text-transform: uppercase !important;
            letter-spacing: 1.5px !important;
            padding: 10px 24px !important;
            transition: all 0.3s ease !important;
        }

            main .btn:hover, main .btn-primary:hover, main .btn-outline-primary:hover, main a.btn:hover {
                background-color: var(--neon-red) !important; /* ÜSTÜNE GELİNCE DOLAR */
                color: #ffffff !important; /* YAZI BEYAZ OLUR */
                box-shadow: 0 0 20px rgba(255, 0, 85, 0.6) !important;
                transform: translateY(-2px);
            }

        /* ARAMA KUTUSU */
        .search-box {
            width: 280px !important; /* Boyunu uzattık (Eskiden 125-160px civarıydı) */
            height: 34px !important; /* Biraz daha etli durması için */
            font-size: 12px !important;
            background: rgba(255, 255, 255, 0.03) !important;
            border: 1px solid rgba(255, 255, 255, 0.1) !important;
            color: #fff !important;
            transition: all 0.3s ease;
        }

            /* Arama kutusuna tıklandığında hafif parlama efekti */
            .search-box:focus {
                width: 320px !important; /* Tıklayınca biraz daha uzasın (Dinamik etki) */
                border-color: var(--neon-red) !important;
                background: rgba(255, 255, 255, 0.07) !important;
                box-shadow: 0 0 10px rgba(255, 0, 85, 0.2);
            }

        .social-icons {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .social-link {
            display: flex;
            align-items: center;
            height: 24px; /* hepsi aynı yükseklik */
        }

            .social-link img {
                height: 18px;
                width: auto;
                filter: brightness(150%);
            }

            .social-link i {
                font-size: 18px; /* spotify ile img eşitlendi */
                color: rgba(255,255,255,0.7);
            }

        .aoty-text {
            font-family: 'Arial Black', sans-serif;
            font-weight: 900;
            font-size: 14px; /* diğerleriyle aynı optik boy */
            color: var(--text-cream);
            letter-spacing: 1px;
        }

        .social-link:hover {
            transform: scale(1.08);
            transition: 0.2s ease;
        }

        /* Spotify hover efekti */
        .social-link:has(.bi-spotify):hover i {
            color: #1DB954 !important;
            filter: drop-shadow(0 0 8px #1DB954);
            transition: all 0.3s ease;
        }

        .social-link .bi-spotify {
            transition: all 0.3s ease;
        }

    </style>
</head>
<body>

    <header>
        <nav class="navbar navbar-expand-lg fixed-top navbar-dark navbar-custom">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center gap-2" asp-area="" asp-controller="Home" asp-action="Index">
                    <span style="color: var(--neon-red); font-size: 14px;">●</span>
                    CinemaMusicBlog
                </a>
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#anaMenu">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="anaMenu">
                    <ul class="navbar-nav ms-auto align-items-center" style="gap: 1.8rem;">
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">Ana Sayfa</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Posts" asp-action="Index" asp-route-category="Sinema">
                                <i class="bi bi-film"></i> Sinema
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Posts" asp-action="Index" asp-route-category="Müzik">
                                <i class="bi bi-music-note-beamed"></i> Müzik
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Posts" asp-action="Index">Arşiv</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Home" asp-action="About">Hakkımda</a>
                        </li>
                    </ul>
                    <div class="d-flex align-items-center gap-3 ms-4">
                        <div class="social-icons d-flex align-items-center gap-3 border-end border-secondary border-opacity-25 pe-3">
                            <a href="https://letterboxd.com/Liebertt/" target="_blank" class="hover-glow social-link">
                                <img src="~/images/icon-letterboxd.png" alt="Letterboxd">
                            </a>
                            <a href="https://open.spotify.com/user/n0fcj8pwvpbdf5cjspwz26sws?si=7d607c92db204199" target="_blank" class="hover-glow social-link">
                                <i class="bi bi-spotify"></i>
                            </a>
                            <a href="https://www.albumoftheyear.org/user/kalsaftar/" target="_blank" class="hover-glow social-link aoty-text">
                                AOTY
                            </a>
                        </div>
                        <form class="d-flex position-relative" asp-area="" asp-controller="Posts" asp-action="Index" method="get">
                            <input type="text" name="searchString"
                                   class="form-control form-control-sm bg-dark text-white border-secondary rounded-pill ps-3 pe-4 search-box"
                                   placeholder="Arşivde ara..." style="width: 200px;">
                            <button type="submit" class="btn btn-sm position-absolute top-50 end-0 translate-middle-y border-0 text-white-50 pe-3">
                                <i class="bi bi-search" style="font-size: 11px;"></i>
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </nav>  
    </header>

    <main role="main" class="container mt-4">
        @RenderBody()
    </main>

    <footer class="text-center py-5 mt-5 border-top border-secondary border-opacity-10 text-white-50 small">
        <div class="container">
            &copy; 2026 - <span class="text-danger fw-bold">CinemaMusicBlog</span> | Tasarım & Kodlama: Sercan Demir
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>